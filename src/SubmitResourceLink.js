import { Link } from "react-router-dom";

const SubmitResourceLink = () => {
  return (
    <div id="submit-resource">
      <Link to="/submit">Click here to submit a new resource</Link>
    </div>
  );
};

export default SubmitResourceLink;
